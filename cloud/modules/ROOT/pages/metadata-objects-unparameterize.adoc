= Unparameterize metadata objects
:page-last_updated: Sep, 10 2026
:author: Mark
:linkattrs:
:experimental:
:page-aliases:
:description: Learn how to remove parameterization variables from tables and connections and restore them to static configurations.
:jira: SCAL-292421

// TODO: verify with engineering — Confirm exact release version for this feature
// This feature is available in ThoughtSpot Cloud __26.3.0.cl or later__.
// ThoughtSpot Software: Not available.
[.lead]
Learn how to remove parameterization variables from tables and connections and restore them to a single, static configuration.

== Overview

In multi-tenant ThoughtSpot deployments, you can use *metadata parameterization* to define variables for metadata objects. This allows different Orgs to use different physical tables or connection properties at runtime.

For example, you might parameterize a connection so that each Org connects to a different database host, or parameterize a table so that different Orgs query different physical tables with the same schema.

When you no longer need this per-Org variation, you can *unparameterize* the metadata object. Unparameterization removes the variables and restores the object to use a single, static configuration shared across all Orgs.

// SOURCE: Jira SCAL-292420, Confluence Template Variables page

== Why unparameterize metadata

You might want to unparameterize a table or connection in the following scenarios:

* You no longer need different physical tables or connection properties per Org. For example, all Orgs are now consolidated into a single shared database.
* You want to simplify your metadata management by removing variables that are no longer needed.
* You are migrating a parameterized object to a new environment where per-Org variation is not required.
* You need to restore an object to its primary-org configuration after testing parameterization.

// SOURCE: Jira SCAL-292420 description

[IMPORTANT]
====
Unparameterization removes all per-Org variable assignments and restores the primary-org values. Any content that relies on the parameterized configuration may be affected.
====

// TODO: verify with engineering — Confirm whether unparameterization is reversible (can the object be re-parameterized after unparameterization?)

== Prerequisites

Before you can unparameterize a table or connection, ensure that you meet the following requirements:

* Publishing must be enabled. Contact your ThoughtSpot {support-url} to enable it.
You have the *Can manage data* privilege or equivalent role.
* You must be an administrator.
* The object is currently parameterized. Only objects that have parameterization variables can be unparameterized.

// TODO: verify with engineering — Confirm exact permissions required

[IMPORTANT]
====
Unparameterization restores primary-org values for all selected objects, including sensitive fields such as passwords and secret keys. Ensure you have the correct primary-org values configured before proceeding.
====

== Unparameterizing tables

You can unparameterize variables from single or multiple tables.

=== Unparameterizing a single table

To remove parameterization from a single table, do the following:

. Navigate to the *Data workspace* tab.
. On the *Data objects* page, select the checkbox for the table you want to unparameterize.
. Click *Manage parameters*.
+
The *Manage parameters* window shows the mapping fields that are parameterized, such as:
+
* *Database name* — mapped to a variable
* *Schema name* — mapped to a variable
* *Table name* — mapped to a variable

. In the *Manage parameters* window, click *Edit*.
. Click the *x* to the right of the fields you want to unparameterize.
. Click *Save*.

After a table is unparameterized, the selected fields are restored to their primary Org values, and all Orgs will use the same physical table.

// SOURCE: scaligent blink-v2 unparameterise-modal.container.tsx, commit SCAL-330038

=== Unparameterizing multiple tables

To remove parameterization variables from multiple tables, do the following:

. Navigate to the *Data workspace* tab.
. On the *Data objects* page, select the checkbox for two or more tables you want to unparameterize.
. Click *Unparameterize*.
. In the *Unparameterize* window, review the table properties that are currently parameterized.
. Select the checkbox for each field you want to unparameterize. (By default, all fields are selected.)
. Click *Unparameterize*.

After tables are unparameterized, their properties are restored to their primary Org values, and all Orgs will use the same physical tables.

== Unparameterizing connections

You can unparameterize variables from single or multiple connections.

=== Unparameterizing a single connection

To remove parameterization on a single connection, do the following:

. Navigate to the *Data workspace* tab.
. Click *Connections* in the left-hand navigation.
. On the *Connections* page, select the checkbox for the connection you want to unparameterize.
. Click *Manage parameters*.
. In the *Manage parameters* window, review the connection properties that are currently parameterized.
+
The window shows the connection properties that are mapped to variables, such as:
+
* *Host*
* *Port*
* *Database*
* *Username*

. Click the *x* to the right of the fields you want to unparameterize.
. Click *Save*.

After a connection is unparameterized, the connection properties are restored to their primary Org values, and all Orgs will use the same connection configuration.

=== Unparameterizing multiple connections

To remove parameterization variables from multiple connections, do the following:

. Navigate to the *Data workspace* tab.
. Click *Connections* in the left-hand navigation.
. On the *Connections* page, select the checkbox for two or more connections you want to unparameterize.
. Click *Unparameterize*.
. In the *Unparameterize* window, review the connection properties that are currently parameterized.
. Select the checkbox for each field you want to unparameterize. (By default, all fields are selected.)
. Click *Unparameterize*.

After connections are unparameterized, their properties are restored to their primary Org values, and all Orgs will use the same connection configurations.

== Unparameterize using the REST API

You can also unparameterize metadata objects programmatically using the REST API v2.0.

For more information, see the following API endpoint:

* `POST /api/rest/2.0/metadata/unparameterize` — Unparameterize a metadata object

// SOURCE: thoughtspot/developer-docs rest-api-v2-reference.adoc

== Troubleshooting

[.faq]
--
Cannot see the unparameterize option::
If you cannot see the *Unparameterize* option in the UI, check the following:

* Ensure the object is parameterized. Only parameterized objects can be unparameterized.
* Verify that you have the required permissions (Can manage data or Can edit on the object).
* If the object is shared with you, the owner may need to grant you Can edit permission.

Unparameterization fails::
If the unparameterization fails, check the following:

* Ensure the primary Org values are valid. If the primary Org values for the table mapping or connection properties are invalid, the unparameterization may fail.
* Check for dependent objects that may be blocking the unparameterization.
* Review the error message for specific details about the failure.

Dependent objects show errors after unparameterization::
If dependent objects show errors after unparameterization:

. Verify that the underlying table or connection is accessible with the primary-org values.
. Check that the dependent objects (Worksheets, Answers, Liveboards) are still valid.
. Rebuild or refresh any affected content as needed.
--

== Related information

* xref:publishing.adoc[Publishing objects between Orgs]
* xref:orgs-overview.adoc[Multi-tenancy with Orgs]
* xref:connections.adoc[Add and edit connections]

== Limitations

* Unparameterization is currently supported for tables and connections only. Other metadata objects (such as Models, Views, and SQL views) cannot be unparameterized using this feature.
* Unparameterization removes all per-Org variable assignments. You cannot selectively unparameterize variables for specific Orgs.
* After unparameterization, all Orgs will use the same primary-org configuration.

