= Delete a table with dependent objects from an {connection} connection
:page-last_updated: 05/05/2026
:linkattrs:
-early-access
:page-aliases:
:experimental:
:connection: Iomete
:description: To delete a table with dependencies from an Iomete connection, first delete the dependent objects.
:jira: SCAL-297520



If you attempt to delete a table with dependent objects, the operation is blocked.
A _Cannot delete_ window appears, with a list of links to dependent objects.

In the _Cannot delete_ window, select the link for each object to modify or delete it.

image::embrace-delete-table-depend.png[Cannot delete table with dependents warning]

You can also click the name of a table and then select the linked objects to see a list of dependent objects with links.
The list shows the names of the dependent objects (Models, Liveboards, or Answers), and the columns they use from that table.
You can use this information to determine the impact of changing the structure of the data source or to see how widely used it is.
Select a dependent object to modify or delete it.

When all dependencies are removed, you can delete the table.
See xref:connections-iomete-delete-table.adoc[Delete a table from an {connection} connection].

'''
> **Related information**
>
> * xref:connections-iomete-add.adoc[Add an {connection} connection]
> * xref:connections-iomete-edit.adoc[Edit an {connection} connection]
> * xref:connections-iomete-remap.adoc[Remap an {connection} connection]
> * xref:connections-iomete-delete-table.adoc[Delete a table from an {connection} connection]
> * xref:connections-iomete-delete.adoc[Delete an {connection} connection]
> * xref:connections-iomete-deactivate.adoc[]
> * xref:connections-iomete-reference.adoc[Connection reference for {connection}]
