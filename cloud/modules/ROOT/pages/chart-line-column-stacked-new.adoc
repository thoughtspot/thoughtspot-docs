= New line stacked column charts
:page-last_updated: Jan 30, 2026
:linkattrs:
:experimental:
:description: The new line stacked column chart is similar to the new line column chart, except that it divides its columns with an attribute in the legend.
:page-layout: default-cloud
:page-partial:

The new line stacked column chart is similar to the line column chart, it divides its columns with an attribute in the legend. The line stacked column chart combines xref:chart-column-stacked-new.adoc[new stacked column] and xref:chart-line-new.adoc[new line] charts.
There are two y-axes, one for each measure.

[.bordered]
image::line-stacked-column-chart-new-example.png[Stacked line column chart example]


== Limitations
* Dual-axis or multivariate axis transitions are not allowed
* At most one measure can be visualized as a bar.
* No more than one measure can be added to the column axis.
* A third axis entity is not allowed.
* Neither axis entities can be removed nor emptied.
* Internal dual-axis configuration is not supported for the line axis.


