= New pie and donut charts
:page-last_updated: Jun 18, 2026
:linkattrs:
:experimental:
:page-layout: default-cloud
:page-aliases:
:description: A pie or donut chart displays data as proportional slices of a circle.
:jira: SCAL-285348

// TODO: verify with engineering — confirm exact page-last_updated date for 26.7 GA release

The new pie and donut charts display data as proportional slices of a circle, where each slice represents a category's share of the total. Donut charts use the same layout with a hollow center, which can be used to display a summary value or label.

These charts are best suited for showing part-to-whole relationships when there are a small number of well-differentiated categories.

[.bordered]
image::chart-donut-new.png[Pie and donut chart example]
// TODO: verify with engineering — provide screenshot (image file: charts-pie-new.png)

Your search needs at least one attribute and one measure to be represented as a pie or donut chart.

== New features

* Switch between pie and donut display styles within the same chart configuration
* Faceting support: When an attribute is added to the trellis section, the chart generates small multiples — one pie or donut per attribute value
+
//[.bordered]
//image::faceted-pie-chart.png[Faceted pie chart]
// TODO: verify with engineering — provide screenshot (image file: faceted-pie-chart.png)
* Granular tooltip customization to control what information appears when hovering over a slice
* Color slicing support to distinguish categories within slices

== Limitations
* Pie and donut charts are best used with a small number of categories. A large number of slices reduces readability significantly.
* Multivariate mode is not supported for pie and donut charts.
// TODO: verify with engineering — confirm full limitations list for pie/donut at 26.7 GA
