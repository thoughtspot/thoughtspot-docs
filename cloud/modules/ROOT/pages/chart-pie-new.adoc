= New pie and donut charts
:page-last_updated: Jun 18, 2026
:linkattrs:
:experimental:
:page-layout: default-cloud
:page-aliases:
:description: A pie or donut chart displays data as proportional slices of a circle.
:jira: SCAL-285348

// TODO: verify with engineering — confirm exact page-last_updated date for 26.7 GA release

The new pie and donut charts display data as proportional slices of a circle, where each slice represents a category's share of the total. Donut charts use the same layout with a hollow center, which can be used to display a summary value or label.

These charts are best suited for showing part-to-whole relationships when there are a small number of well-differentiated categories.

[.bordered]
image::chart-donut-new.png[Pie and donut chart example]
// TODO: verify with engineering — provide screenshot (image file: charts-pie-new.png)

Your search needs at least one attribute and one measure to be represented as a pie or donut chart.

== New features

* Switch between pie and donut display styles within the same chart configuration
* Faceting support: When an attribute is added to the trellis section, the chart generates small multiples — one pie or donut per attribute value
+
//[.bordered]
//image::faceted-pie-chart.png[Faceted pie chart]
// TODO: verify with engineering — provide screenshot (image file: faceted-pie-chart.png)
* Granular tooltip customization to control what information appears when hovering over a slice
* Color slicing support to distinguish categories within slices
* Allowed configurations:
** 1 attribute + 2 measures
** N attributes + 1 measure
** N attributes + 2 measures
** Values chip + N measures
* Values chip can be added as the last attribute chip in all the above cases
* Case: 1 attribute + 2 measures
** Creates 2 concentric donuts - one for each measure
* Case: N attributes + 1 measure
** Creates faceted donuts
** The last attribute is used to create arcs for the pies
** Other attributes are used to create facets
* Case: N attributes + 2 measures
** Creates faceted concentric donuts
** The 2 measures are used to create 2 concentric donuts
** Last attribute is used to create arcs
** Other attributes used to create facets
* Case: Values chip + N measures
** Creates a single donut chart with arcs as different measures
** Example: A donut showing split of M1,M2,M3 …

Additional settings to configure inner / outer radius for each layer:

* In case of multiple measures in pie/donut chart: The inner chart remains a pie chart and outer chart becomes a donut chart
* The difference between outer radius of inner mark and inner radius of outer mark is the distance between the two layers
* Validations: (Show warnings, but allow applying settings to the charts)
** The outer radius of the inner mark cannot be greater than the inner radius of the outer mark
** The outer radius of the inner mark cannot be greater than outer radius of the outer mark

=== Value chips in category

image::val-chips-cat1.png[]

image::val-chips-cat2.png[]

=== Radius control

image::rad-control-1.png[]

image::rad-control-2.png[]

=== Pie mode (set inner radius to zero)

image::pie-inner-rad-1.png[]

image::pie-inner-rad-2.png[]


=== Trellis by an attribute

image::trellis-at-1.png[]

image::trellis-at-2.png[]




== Limitations
* Pie and donut charts are best used with a small number of categories. A large number of slices reduces readability significantly.
* Multivariate mode is not supported for pie and donut charts.
// TODO: verify with engineering — confirm full limitations list for pie/donut at 26.7 GA
