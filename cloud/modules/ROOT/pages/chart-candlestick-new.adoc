= Candlestick charts
:page-last_updated: September 22, 2026
:linkattrs:
:experimental:
:page-aliases:
:description: Candlestick charts in ThoughtSpot's new chart library
:jira: SCAL-299456, SCAL-299457

++++
<style>
.tabs {
  margin-top: 1em;
}
</style>
++++

Candlestick charts display price movements over time, showing the open, high, low, and close (OHLC) values for each time period. They are commonly used in financial and trading analysis to visualize market trends, volatility, and price patterns.

[.bordered]
image::new_candlestick_chart_example.png[Candlestick chart example image]

// SOURCE: Jira SCAL-299457 — "Candlestick is part of the Muze Set 2 Charts EA rollout"

[NOTE]
====
Candlestick charts are available as part of the Muze Set 2 Charts Early Access rollout. Contact {support-url} to enable this chart type on your instance.
====

== Data requirements

To create a Candlestick chart, you need:

* At least one measure
* At least one attribute (typically a date or time field)

// TODO: verify with engineering — confirm exact data requirements for Candlestick chart (Jira mentions "Labelling for OHLC values" suggesting specific measure configuration)

== New capabilities

The Muze-powered Candlestick chart introduces the following capabilities not available in the classic version:

// SOURCE: Jira SCAL-299457 — New Capabilities section

* *New axis configuration logic for multivariates and grouped axis* — Build complex multivariate Candlestick charts with improved axis handling.
* *Faceting and Trellis chart modes* — Partition Candlestick charts into faceted grids or trellis small multiples for multidimensional analysis.
* *Labelling for OHLC values* — Enhanced labeling support for open, high, low, and close values on the chart.

== Behavior changes

When migrating from the classic Candlestick chart to the Muze-powered version, note the following changes:

// SOURCE: Jira SCAL-299457 — Existing Feature Gaps / Behavior Changes section

* *Multiple attributes create facets* — When you add multiple attributes to a Candlestick chart, the new renderer creates facets instead of combined slices. This is consistent across all Muze-powered charts. You can use a formula to concatenate fields if you need a single combined axis.
* *Custom font support* — Custom font settings apply differently compared to classic charts.
* *Null and empty handling* — Null and empty values are handled differently in the new renderer.
* *Non-visualized fields in tooltip* — Adding non-visualized fields to tooltips is not supported in the new Candlestick chart.

== Limitations

The following features are not supported in the new Candlestick chart:

// SOURCE: Jira SCAL-299457 — Won't Do section

* Text backgrounds, borders, and alignment
* Data label styling and text backgrounds, borders, and alignment

// TODO: verify with engineering — confirm whether "Text backgrounds/borders/alignment" and "Data label styling" are two separate limitations or one combined limitation

== Configure a Candlestick chart

To create a Candlestick chart:

. Start a new search or open an existing Answer.
. In the chart selector panel, select *Candlestick* from the *New* section.
. Add the required measures and attributes to your query.
. The chart renders each data point as a candlestick showing the OHLC values.
. Use the chart settings panel to configure faceting, trellis, and axis options.

// TODO: verify with engineering — confirm exact measure configuration for OHLC values in Candlestick chart
